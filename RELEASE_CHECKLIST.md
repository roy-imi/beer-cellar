# 啤记 1.0 上线清单

代码侧已经完成发布态清理、隐私授权、异常兜底、数据隔离、照片备份恢复和版本更新提示。下面项目必须由小程序管理员在微信/腾讯云后台完成。

## 当前后台状态（2026-08-24）

- [x] 已创建免费 CloudBase 环境 `beer-cellar-prod-d7ew43z02bc0263`（上海），并写入小程序初始化配置。
- [x] 已在 TokenHub 广州启用 `glm-5v-turbo` 免费体验，后付费保持关闭。
- [x] 已创建仅可访问该模型的专用 Key；Key 只保存在云函数环境变量中。
- [x] 已部署 `recognizeBeerLabel` 云函数并完成真实 TokenHub 调用验证。
- [x] 已将现有免费环境转换为微信云开发环境；CloudBase 小程序认证已于 2026-08-24 13:21:44 显示授权成功。
- [ ] 微信公众平台正式版与体验版隐私保护指引待管理员填写并提交。
- [ ] iPhone、Android 真机验收与微信开发者工具上传待完成。

## 一、微信公众平台

1. 确认 AppID `wx52537f83e1d5b5dc` 属于准备发布的“啤记”小程序，补齐头像、简介和与实际功能一致的服务类目。
2. 在“小程序用户隐私保护指引”中如实声明：用户选择的照片、相机拍摄的酒标照片，以及经 CloudBase 云函数调用 TokenHub 进行酒标信息提取的目的。可参考 [PRIVACY_GUIDE_TEMPLATE.md](./PRIVACY_GUIDE_TEMPLATE.md)。
3. 同时配置体验版隐私指引；否则开发版正常、体验版/正式版可能无法调用照片相关接口。
4. 如果后台在提审时检测到更多隐私接口，以实际检测结果为准补充说明，不要勾选代码没有使用的信息类型。
5. 确认小程序不提供酒类交易、导购或未成年人饮酒内容；当前版本只是个人记录工具。

## 二、CloudBase

1. [已完成] “啤记”已开通微信云开发并绑定现有环境 `beer-cellar-prod-d7ew43z02bc0263`；CloudBase“小程序认证”状态为授权成功，代码已显式初始化该环境。
2. [已完成] `recognizeBeerLabel` 云函数已部署，TokenHub 专用 Key 已配置为服务端环境变量 `TOKENHUB_API_KEY`，未写入客户端源码。
3. [已完成] TokenHub 广州地域的 `glm-5v-turbo` 为“运行中 / 免费体验”，后付费关闭。
4. 设置资源用量告警并定期检查免费额度；额度耗尽时应让识别服务停止，不能自动转后付费。
5. 在真机体验版用清晰正标、背标各测试一次；确认能识别酒名、酒厂、容量，并且“最佳饮用日期”不会被写成生产日期。

## 三、提交前真机验收

1. 运行 `npm run check:release`，必须全部通过。
2. iPhone 和 Android 各走通一次：首次启动 → 拒绝隐私授权 → 手动入库 → 再次扫码并同意授权。
3. 验证空酒窖、入库、编辑、喝掉一瓶、喜欢、删除、酒吧记录和清空数据。
4. 生成带照片的完整备份，发送到微信文件传输助手，再从该文件恢复；核对记录数量和照片。
5. 断网启动一次，确认除酒标识别外的本地功能可用。
6. 在微信开发者工具执行“代码质量”检查，并清理控制台错误。

## 四、上传、审核、发布

1. 微信开发者工具选择“上传”，版本号填写 `1.0.0`，备注写明“个人啤酒库存与饮用记录；包含用户主动触发的酒标图片识别”。
2. 在微信公众平台的版本管理中提交审核，按页面要求再次确认服务类目和隐私保护指引。
3. 审核通过后手动点击发布。
4. 发布后用正式版重新验证扫码、备份文件发送和导入；随后观察 CloudBase 云函数、TokenHub 调用日志与费用告警。

## 暂不阻塞 1.0 的事项

- 自动跨设备同步：1.0 使用用户主动生成的携照片备份完成迁移。
- Untappd 自动查询：当前只保留手动填写评分，未配置的查询入口已隐藏。
- 多用户社交、酒类交易和推荐：不属于 1.0 的产品范围。
