# 小程序隐私保护指引填写参考

这是一份后台填写参考，不替代主体自己的法律审核。方括号内容需要管理员补齐。

## 基本信息

- 开发者/运营者：[主体全称]
- 联系方式：[邮箱或电话]
- 产品名称：啤记
- 产品用途：个人啤酒库存、饮用与酒吧记录管理

## 实际处理的信息

### 用户选择或拍摄的照片

- 使用场景一：用户主动为库存或酒吧记录选择照片，照片默认保存在小程序本机文件空间，用于记录展示和用户主动生成的备份。
- 使用场景二：用户主动点击“拍照识别”后，所选酒标照片经腾讯云 CloudBase 云函数发送至 TokenHub 的 `glm-5v-turbo`，用于识别酒名、酒厂、风格、容量和生产/罐装日期。
- 处理原则：不自动入库；识别结果由用户核对并确认。用户可移除照片、删除单条记录，或在“更多功能 → 缓存管理”清空全部本地数据。

### 本地业务记录

- 包含库存、数量、生产/罐装日期、评分、备注、喜欢状态和酒吧记录。
- 默认只保存在当前设备。用户主动导出时，数据和照片会写入其备份文件；用户主动选择“发送到微信”时才会调起微信文件发送能力。

## 第三方处理说明

- 服务方：腾讯云计算（北京）有限责任公司；使用 CloudBase 云函数和 TokenHub（以控制台协议显示的实际签约主体为准）
- 场景：酒标图片理解
- 目的：从用户主动选择的酒标照片中提取本次入库字段
- 传输方式：小程序调用 CloudBase 云函数，再由云函数通过 HTTPS 调用 TokenHub；TokenHub Key 仅保存在服务端环境变量
- 处理范围：本次选择的 1–2 张酒标照片及固定的字段提取提示词
- 当前资源地域：CloudBase 上海、TokenHub 广州；如后台配置调整，应同步更新本说明
- 应用侧保存：云函数不把酒标照片写入数据库或云存储，只把模型提取结果返回给用户核对
- 保存期限与日志策略：按实际 CloudBase 套餐、控制台配置和服务协议填写，不要自行承诺代码无法保证的期限

## 后台可用的简短目的说明

- 选中的照片：用于保存用户主动添加的啤酒记录，以及在用户主动点击“拍照识别”时提取酒标信息。
- 摄像头：用于拍摄用户主动录入的啤酒酒标，并提取酒名、酒厂、风格、容量和日期等信息。
- 第三方处理：用户主动选择的 1–2 张酒标照片会经腾讯云 CloudBase 云函数发送至 TokenHub，仅用于完成本次识别；识别结果由用户核对后决定是否保存。

## 用户权利与退出

- 不同意照片相关授权时，用户仍可使用手动入库和其他本地记录功能。
- 用户可删除单条记录与照片，也可清空全部本地数据。
- 用户可通过[联系方式]申请咨询、投诉或行使相关权利。

## 未成年人提示

啤记不提供酒类购买或交易服务。产品内应保留“理性饮酒，未成年人请勿饮酒”的提示。
